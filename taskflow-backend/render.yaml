services:
  - type: web
    name: taskflow-backend
    runtime: node
    rootDir: taskflow-backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
